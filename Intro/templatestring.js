/*

 */
let firstName="kijana"
let secondname="uhuru"
let age=20
let address="eldoret"

let userdetails=firstName+secondname+"is"+age+"years old"+"he stays at"+address
console.log(userdetails)
console.log(" user details is",typeof userdetails)
///
let userdetails2=
`${firstName} ${secondname} is ${age} years old and stays at ${address}`
console.log(userdetails2)
console.log(`$firstname} ${secondname} is ${age} years old and he stay at ${address}`)